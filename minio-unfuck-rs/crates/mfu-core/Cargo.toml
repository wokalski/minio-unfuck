[package]
name = "mfu-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Re-export minio-format for MinIO-specific parsing
minio-format = { git = "https://github.com/dialohq/minio-format-rs" }

# Core dependencies
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
tracing = { workspace = true }
nix = { workspace = true }

# Database
duckdb = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
io-uring = { workspace = true }

[dev-dependencies]
tempfile = "3"
